import styles from './footer.module.css';

function Footer() {
    return (
        <footer className={styles.footer}>
            <h2> Desenvolvimento teste React</h2>
        </footer>
    );
}

export default Footer;